(executables
  (names public student properties)
  (libraries p3 qcheck oUnit))

(alias
  (name runtest)
  (action
    (progn (run ./public.bc)
           (run ./student.bc)
           (run ./properties.bc))))
